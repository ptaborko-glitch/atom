# Проект Atom — Конец дня 2 июня 2026

## Что работает
- ✅ Лексер на Atom (скомпилирован в `output.wasm`)
- ✅ Парсер на JavaScript для одного выражения
- ✅ Генератор WAT для одного выражения
- ✅ Полный цикл: `1+2` → WASM → результат `3`

## Что не работает (требует исправления lexer.atom)
- ❌ Многострочные программы (не возвращается EOF)
- ❌ Ключевые слова
- ❌ Сохранение имён переменных

## Следующие шаги (завтра)
1. Исправить `lexer.atom` для корректной работы с EOF
2. Или полностью перейти на JavaScript компилятор
3. Написать `parser.atom` после исправления лексера

## Текущие файлы
- `output.wasm` — рабочий лексер (НЕ ПЕРЕКОМПИЛИРОВАТЬ!)
- `working_compiler6.js` — компилятор для одного выражения
- `multi_statement_compiler5.js` — компилятор для нескольких строк (не работает)

---

**Статус:** 🟡 Фундамент готов, нужно исправить лексер

**Завтра:** Начинаем с отладки `lexer.atom`
