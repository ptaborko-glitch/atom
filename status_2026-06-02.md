# Проект Atom — Статус разработки

**Дата:** 2026-06-02
**Среда:** GitHub Codespace
**Статус:** 🟢 Лексер стабильно работает

---

## Что сделано сегодня

### Лексер на Atom (`lexer.atom`)
- ✅ Компилируется в `output.wasm`
- ✅ Распознаёт числа, операторы (`+`, `-`, `*`, `/`, `=`, `<`, `>`, `:`), идентификаторы
- ✅ Корректно обрабатывает пробелы и новую строку
- ✅ Возвращает `TOKEN_EOF` в конце файла
- ✅ Успешно разбирает `x = 42 + 1` → `IDENT, ASSIGN, NUMBER, PLUS, NUMBER, EOF`

### Тестирование
- ✅ `test_tokens_full.js` — проверка всех токенов
- ✅ `test_lexer_fixed.js` — проверка отдельного символа `+`

---

## Что не получилось
- ❌ Ключевые слова (`if`, `else`, `while`, `return`, `func`) — лексер зависает
- ❌ Сохранение имён переменных в `lexer_token_value`

**Решение:** Отложено на потом. Для парсера выражений ключевые слова пока не нужны.

---

## План на завтра (День 1)

### Задача: Написать парсер на Atom для выражений

1. Создать `parser.atom`
2. Реализовать базовые функции:
   - `next_token()` — получение следующего токена
   - `eat(expected)` — проверка и продвижение
   - `peek(expected)` — проверка типа токена
3. Реализовать парсинг выражений:
   - `parse_number()`, `parse_identifier()`, `parse_primary()`
   - `parse_unary()`, `parse_multiplication()`
   - `parse_addition()`, `parse_comparison()`
4. Протестировать на `x = 42 + 1`

---

## Команды для работы

```bash
# Компиляция лексера
python3 main_debug2.py
wat2wasm output.wat -o output.wasm

# Тест токенов
node test_tokens_full.js
Следующая сессия: 2026-06-03, утро

Обновлено: 2026-06-02 20:00 UTC
